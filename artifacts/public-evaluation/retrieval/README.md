# Historical Retrieval v1 evidence

This provenance manifest maps to the byte-identical frozen 50-query v1 evidence at
`artifacts/evaluation/`. It remains available as a historical baseline. The current public
benchmark is `../retrieval-v2/`; run `python scripts/verify_retrieval_v2_evidence.py` to verify the
200-query resume metrics.
