# Retrieval evidence

The public provenance manifest maps to the byte-identical frozen 50-query retrieval evidence at `artifacts/evaluation/`. Run `python scripts/verify_retrieval_evidence.py` for no-model verification.
